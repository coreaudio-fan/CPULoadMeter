import SwiftUI

///	The main window's content. A placeholder until the header and the graphs arrive.
struct MainView: View {

	var body: some View {
		Text("CPULoadMeter")
			.frame(minWidth: 480, minHeight: 240)
	}

}
